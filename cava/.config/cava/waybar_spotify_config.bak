[general]
bars = 8
sleep_timer = 0.1
[input]
method = pulse
source = $(pactl list short sinks | awk '/spotify/ {print $2}' 2>/dev/null || echo 'auto')
[output]
method = raw
raw_target = /tmp/waybar_cava_output
data_format = ascii
ascii_max_range = 3
